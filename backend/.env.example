# ============================================================================
# ATRIX E-Commerce Platform - Production Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env with real secrets to Git!
# ============================================================================

# ----------------------------------------------------------------------------
# Server Configuration
# ----------------------------------------------------------------------------
NODE_ENV=production
# PORT is automatically set by Render - leave empty or set for local testing
PORT=3000

# Base URL for the customer-facing website (used for emails, redirects, etc.)
BASE_URL=https://atrixstore.tech

# ----------------------------------------------------------------------------
# Database Configuration - Neon PostgreSQL
# ----------------------------------------------------------------------------
# Format: postgresql://user:password@host:port/database?sslmode=require
# Get this from your Neon dashboard
DATABASE_URL=postgresql://your_user:your_password@your-host.neon.tech/your_database?sslmode=require

# ----------------------------------------------------------------------------
# JWT Configuration
# ----------------------------------------------------------------------------
# Generate with: openssl rand -base64 32
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=7d

# Refresh token (optional - if implementing refresh tokens)
JWT_REFRESH_SECRET=your_refresh_token_secret_here
JWT_REFRESH_EXPIRES_IN=30d

# ----------------------------------------------------------------------------
# Password Hashing
# ----------------------------------------------------------------------------
BCRYPT_SALT_ROUNDS=12

# ----------------------------------------------------------------------------
# Stripe Payment Configuration
# ----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_...) for staging, live keys (sk_live_...) for production
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe Webhook Secret for checkout.session.completed events
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Success/Cancel URLs (where customers are redirected after payment)
STRIPE_SUCCESS_URL=https://atrixstore.tech/orders/success
STRIPE_CANCEL_URL=https://atrixstore.tech/cart

# ----------------------------------------------------------------------------
# Frontend URLs (for CORS configuration)
# ----------------------------------------------------------------------------
# Admin dashboard URL
ADMIN_URL=https://admin.atrixstore.tech

# Customer website URL
CUSTOMER_URL=https://atrixstore.tech

# ----------------------------------------------------------------------------
# Rate Limiting Configuration (optional)
# ----------------------------------------------------------------------------
# Time window for rate limiting in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window per IP
RATE_LIMIT_MAX_REQUESTS=100

# ----------------------------------------------------------------------------
# Optional: Debugging and Logging
# ----------------------------------------------------------------------------
# Set to 'true' to enable detailed order logging
# DEBUG_ORDERS=false

# ----------------------------------------------------------------------------
# Development-Only Settings (DO NOT USE IN PRODUCTION)
# ----------------------------------------------------------------------------
# Uncomment these only for local development
# NODE_ENV=development
# BASE_URL=http://localhost:3000
# ADMIN_URL=http://localhost:5174
# CUSTOMER_URL=http://localhost:3000
# STRIPE_SUCCESS_URL=http://localhost:3000/orders/success
# STRIPE_CANCEL_URL=http://localhost:3000/cart
