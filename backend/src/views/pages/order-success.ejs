<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Successful - ATRIX Store</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { user }) %>

    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6 text-center">
                <div class="success-card bg-white rounded-4 shadow-lg p-5">
                    <!-- Success Animation -->
                    <div class="success-icon-wrapper mb-4">
                        <div class="success-circle">
                            <i class="bi bi-check-lg"></i>
                        </div>
                    </div>

                    <h1 class="mb-3" style="font-family: 'Playfair Display', serif;">Order Successful!</h1>
                    <p class="lead text-muted mb-4">
                        Thank you for your purchase. Your order has been successfully placed and payment confirmed.
                    </p>

                    <!-- Order Info -->
                    <% if (order) { %>
                    <div class="order-info bg-light rounded-3 p-4 mb-4">
                        <div class="row g-3">
                            <div class="col-6 text-start">
                                <small class="text-muted d-block">Order Number</small>
                                <strong class="fs-5" style="color: var(--accent);">#<%= order.orderNumber %></strong>
                            </div>
                            <div class="col-6 text-end">
                                <small class="text-muted d-block">Total Paid</small>
                                <strong class="fs-5">$<%= parseFloat(order.total).toFixed(2) %></strong>
                            </div>
                        </div>
                        <hr class="my-3">
                        <div class="row g-3">
                            <div class="col-6 text-start">
                                <small class="text-muted d-block">Status</small>
                                <span class="badge bg-success"><%= order.status.charAt(0).toUpperCase() + order.status.slice(1) %></span>
                            </div>
                            <div class="col-6 text-end">
                                <small class="text-muted d-block">Date</small>
                                <strong><%= new Date(order.createdAt).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' }) %></strong>
                            </div>
                        </div>
                    </div>
                    <% } else { %>
                    <div class="order-info bg-light rounded-3 p-4 mb-4">
                        <div class="d-flex align-items-center justify-content-center gap-2">
                            <div class="spinner-border spinner-border-sm text-primary" role="status"></div>
                            <span class="text-muted">Processing your order...</span>
                        </div>
                        <p class="small text-muted mt-2 mb-0">Your order confirmation will be sent to your email shortly.</p>
                    </div>
                    <% } %>

                    <!-- Confirmation Message -->
                    <div class="confirmation-message bg-blue-50 rounded-3 p-4 mb-4" style="background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 100%);">
                        <i class="bi bi-envelope-check fs-3 mb-2 d-block" style="color: #3b82f6;"></i>
                        <p class="mb-0 text-muted">
                            A confirmation email has been sent to <strong><%= user.email %></strong> with your order details.
                        </p>
                    </div>

                    <!-- What's Next -->
                    <div class="whats-next text-start mb-4">
                        <h6 class="fw-bold mb-3">What happens next?</h6>
                        <div class="timeline">
                            <div class="timeline-item d-flex gap-3 mb-3">
                                <div class="timeline-icon bg-success text-white rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; min-width: 32px;">
                                    <i class="bi bi-check"></i>
                                </div>
                                <div>
                                    <strong>Order Confirmed</strong>
                                    <p class="text-muted small mb-0">Your payment has been processed</p>
                                </div>
                            </div>
                            <div class="timeline-item d-flex gap-3 mb-3">
                                <div class="timeline-icon bg-light text-muted rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; min-width: 32px;">
                                    <i class="bi bi-box-seam"></i>
                                </div>
                                <div>
                                    <strong>Processing</strong>
                                    <p class="text-muted small mb-0">We're preparing your order</p>
                                </div>
                            </div>
                            <div class="timeline-item d-flex gap-3">
                                <div class="timeline-icon bg-light text-muted rounded-circle d-flex align-items-center justify-content-center" style="width: 32px; height: 32px; min-width: 32px;">
                                    <i class="bi bi-truck"></i>
                                </div>
                                <div>
                                    <strong>Shipped</strong>
                                    <p class="text-muted small mb-0">You'll receive tracking info via email</p>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Action Buttons -->
                    <div class="d-grid gap-3">
                        <a href="/orders" class="btn-premium btn-primary-premium">
                            <i class="bi bi-list-check me-2"></i>View My Orders
                        </a>
                        <a href="/shop" class="btn-premium btn-outline-premium">
                            <i class="bi bi-shop me-2"></i>Continue Shopping
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <style>
    .success-icon-wrapper {
        display: flex;
        justify-content: center;
    }
    .success-circle {
        width: 100px;
        height: 100px;
        background: linear-gradient(135deg, #10b981 0%, #059669 100%);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        animation: scaleIn 0.5s ease-out;
    }
    .success-circle i {
        font-size: 3rem;
        color: white;
        animation: checkmark 0.5s ease-out 0.3s both;
    }
    @keyframes scaleIn {
        from { transform: scale(0); opacity: 0; }
        to { transform: scale(1); opacity: 1; }
    }
    @keyframes checkmark {
        from { transform: scale(0); }
        to { transform: scale(1); }
    }
    .timeline-item:not(:last-child) .timeline-icon::after {
        content: '';
        position: absolute;
        left: 50%;
        top: 100%;
        width: 2px;
        height: 30px;
        background: #e5e7eb;
        transform: translateX(-50%);
    }
    .timeline-item {
        position: relative;
    }
    </style>

    <%- include('../partials/footer') %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
